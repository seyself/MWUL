using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace App 
{
	public class OSXStartDisplayChanger : MonoBehaviour
	{
		void Awake ()
		{
			// DisplayConfig config = ConfigLoader.Load<DisplayConfig>("Data/display.yaml");
			// if (config.startMonitor < 0) config.startMonitor = 0;
			// PlayerPrefs.SetInt("UnitySelectMonitor", config.startMonitor);
		}

		void Update ()
		{
			
		}
	}
}
